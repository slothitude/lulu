# Goal

Create a PNG image called "test.png" in the workspace with the following content:
- A dark blue background (#1a1a2e)
- A large red filled rectangle at position (100, 100) with size 200x100
- A green circle at position (400, 300) with radius 80
- A white line from (50, 400) to (550, 400)
- Yellow text "BashAgent + SDL3" at position (150, 250)

Use the sdl3_render tool with action "render" to create the image. Then verify the file exists using list_files.
